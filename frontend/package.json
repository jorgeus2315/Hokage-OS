{
  "name": "hokage-os-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify:no-secrets": "! grep -rqE 'VITE_ADMIN_TOKEN|OPENROUTER_API_KEY|x-admin-token' dist/assets 2>/dev/null && echo 'bundle OK: sin ADMIN_TOKEN ni secretos' || (echo 'ERROR: secreto detectado en el bundle' && exit 1)"
  },
  "dependencies": {
    "pixi.js": "^8.19.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}
